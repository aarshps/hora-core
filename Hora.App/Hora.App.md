# Hora.App

- Frontend lives in `Hora.App/hora`.
- Main entry points are `package.json`, `src/main.ts`, and files under `src/app/`.
- The app is currently pinned to Angular `21.2.2`; repo-specific security overrides live in `package.json` and `package-lock.json`.
- Standard scripts are `npm start`, `npm run build`, and `npm test`.
- Read [`AGENTS.md`](../AGENTS.md) and [`docs/agent-resume.md`](../docs/agent-resume.md) before repo-specific GitHub or security work.
